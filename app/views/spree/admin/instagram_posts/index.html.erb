<%= render 'page_actions' %>

<% if @instagram_settings.access_token.present? %>
  <%= render 'navigation', active: :user %>
  <%= render 'table', instagram_posts: @instagram_posts, hashtag: false %>
<% else %>
  <%= render 'no_connected' %>
<% end %>
